# `squash` [NOT YET IMPLEMENTED]

> **Status: stub.** The `squash` command checks preconditions (pending count, applied
> migrations) but does **not** yet consolidate migrations. Full squash logic is planned.

## Usage

```bash
dbwarden squash --database primary
```

## Options

- `--database`, `-d`
- `--verbose`, `-v`
